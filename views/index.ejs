<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Upload to S3</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css" />
</head>
<body>

  <div class="background"></div>

  <div class="upload-container">
    <div class="upload-card">

      <h2>🚀 Upload Your File</h2>
      <p class="subtitle">Securely upload files to AWS S3</p>

      <% if (message) { %>
        <div class="message"><%= message %></div>
      <% } %>

      <form action="/upload" method="POST" enctype="multipart/form-data">
        <label for="file-upload" class="file-drop">
          <input id="file-upload" type="file" name="file" required />
          <span class="icon">📂</span>
          <span class="text">Click or drag a file here</span>
          <span id="file-name" class="filename"></span>
        </label>

        <button type="submit">Upload File</button>
      </form>

      <div class="footer">
        Powered by <strong>AWS S3</strong>
      </div>

    </div>
  </div>

  <script>
    const fileInput = document.getElementById("file-upload");
    const fileName = document.getElementById("file-name");

    fileInput.addEventListener("change", () => {
      if (fileInput.files.length > 0) {
        fileName.textContent = fileInput.files[0].name;
      }
    });
  </script>

</body>
</html>
